---
receiver: slack_send_resolved
group_by:
  - alertname
  - location
group_wait: 5s
group_interval: 2m
repeat_interval: 12h
routes:
  - receiver: slack_dont_send_resolved
    matchers:
      - severity = info
