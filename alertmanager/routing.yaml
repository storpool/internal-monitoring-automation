---
receiver: slack_send_resolved
group_by:
  - alertname
  - location
  - job
group_wait: 15s
group_interval: 4m
repeat_interval: 6h
routes:
  - receiver: slack_dont_send_resolved
    matchers:
      - severity = info
