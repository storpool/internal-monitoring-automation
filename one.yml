plugin: "netcho.opennebula.opennebula"
compose:
  ansible_host: nic[0].ip
  running: state == 'active' and lcm_state == 'running'
groups:
  moonstone_monitor: 'user_attributes.labels is defined and "monitor" in user_attributes.labels'
