plugin: netbox.netbox.nb_inventory
validate_certs: true
config_context: false
plurals: false
group_by:
  - site
groups:
  sof_baremetal: role == "server" and site == "3dc"
  sof_baremetal_deploy: '"monitor-autodeploy" in tags'
