{% if 'icmp_monitor' in groups %}
{% for host in groups['icmp_monitor'] %}
- targets:
    - {{ (lookup('dig', hostvars[host]['ansible_fqdn']) | first)['A'] }}
  labels:
    instance: {{ host }}
    location: {{ hostvars[host]['site'] }}
    environment: production
{% endfor %}
{% endif %}