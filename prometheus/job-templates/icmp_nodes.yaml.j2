{% if 'monitor_nodes' in groups %}
{% for host in groups['monitor_nodes'] %}
- targets:
    - {{ hostvars[host]['ansible_host'] }}
  labels:
    instance: {{ host }}
    location: 3dc
    environment: production
{% endfor %}
{% endif %}