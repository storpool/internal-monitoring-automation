apiVersion: operator.victoriametrics.com/v1beta1
kind: VMScrapeConfig
metadata:
  name: jenkins
  namespace: internal-monitoring
  labels:
    storpool.com/env: production
    storpool.com/managed-by: internal-monitoring-automation
spec:
  honorLabels: true
  path: /prometheus
  staticConfigs: {{ lookup('template', 'manifests/monitoring/targets/jenkins_service.yaml.j2') | from_yaml or [] }}
