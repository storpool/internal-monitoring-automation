{% for host in groups["postgresql_hosts"] %}
- targets:
    - {{ hostvars[host]["ansible_host"] }}:9187
  labels:
    instance: {{ host }}
    location: moonstone
    environment: production
{% endfor %}
