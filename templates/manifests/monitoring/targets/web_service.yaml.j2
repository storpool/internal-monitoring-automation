{% for host in (groups['web_service'] | default([])) %}
- targets:
{% for url in hostvars[host]["healthcheck_url"] %}
    - {{ url }}
{% endfor %}
  labels:
    instance: {{ host }}
    location: {{ hostvars[host]["location"] | default("undefined") }}
    environment: production
{% endfor %}
