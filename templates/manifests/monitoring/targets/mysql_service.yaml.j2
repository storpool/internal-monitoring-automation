{% for host in (groups['mysql_hosts'] | default([]))  %}
- targets:
    - {{ hostvars[host]["ansible_host"] }}:9104
  labels:
    instance: {{ host }}
    location: moonstone
    environment: production
{% endfor %}
